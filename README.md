# go-concurrent-log-pipeline
Concurrent log parser CLI in Go – reads timestamp|level|message logs from file or stdin, processes lines via a worker pool pipeline, and outputs aggregated counts per level. 
